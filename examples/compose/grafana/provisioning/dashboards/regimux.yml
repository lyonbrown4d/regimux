apiVersion: 1

providers:
  - name: RegiMux
    orgId: 1
    folder: RegiMux
    folderUid: regimux
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
